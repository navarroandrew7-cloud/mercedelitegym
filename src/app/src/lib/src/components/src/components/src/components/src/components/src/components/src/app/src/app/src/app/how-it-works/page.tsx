import Section from "@/components/Section";

export default function HowItWorks() {
  return (
    <Section title="How It Works">
      <p className="max-w-xl text-gray-600">
        We start with a 1-on-1 consultation, guide strength training, support recovery,
        and track progress so nothing is left to chance.
      </p>
    </Section>
  );
}
